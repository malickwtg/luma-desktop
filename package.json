{
  "name": "luma-desktop",
  "version": "0.1.4",
  "private": true,
  "description": "LUMA Desktop — Tauri shell that loads luma.waytogrow.es and bridges a Claude Agent SDK sidecar to the LUMA MCP.",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "sidecar:build": "pnpm --dir sidecar build",
    "sidecar:check": "pnpm --dir sidecar typecheck"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "packageManager": "pnpm@10.30.0"
}
